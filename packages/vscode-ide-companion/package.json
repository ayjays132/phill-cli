{
  "name": "phill-cli-vscode-ide-companion",
  "displayName": "Phill CLI Companion",
  "description": "Enable Phill CLI with direct access to your IDE workspace.",
  "version": "1.2.0",
  "publisher": "phill",
  "icon": "assets/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/ayjays132/phill-cli.git",
    "directory": "packages/vscode-ide-companion"
  },
  "engines": {
    "vscode": "^1.99.0"
  },
  "license": "LICENSE",
  "preview": true,
  "categories": [
    "AI"
  ],
  "keywords": [
    "phill-cli",
    "phill cli",
    "phill",
    "phill code",
    "cli",
    "ide integration",
    "ide companion"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "configuration": {
      "title": "Phill CLI",
      "properties": {
        "phill-cli.debug.logging.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable detailed logging for debugging the Phill CLI Companion."
        }
      }
    },
    "languages": [
      {
        "id": "phill-diff-editable"
      }
    ],
    "commands": [
      {
        "command": "phill.diff.accept",
        "title": "Phill CLI: Accept Diff",
        "icon": "$(check)"
      },
      {
        "command": "phill.diff.cancel",
        "title": "Phill CLI: Close Diff Editor",
        "icon": "$(close)"
      },
      {
        "command": "phill-cli.runPhillCLI",
        "title": "Phill CLI: Run"
      },
      {
        "command": "phill-cli.showNotices",
        "title": "Phill CLI: View Third-Party Notices"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "phill.diff.accept",
          "when": "phill.diff.isVisible"
        },
        {
          "command": "phill.diff.cancel",
          "when": "phill.diff.isVisible"
        }
      ],
      "editor/title": [
        {
          "command": "phill.diff.accept",
          "when": "phill.diff.isVisible",
          "group": "navigation"
        },
        {
          "command": "phill.diff.cancel",
          "when": "phill.diff.isVisible",
          "group": "navigation"
        }
      ]
    },
    "keybindings": [
      {
        "command": "phill.diff.accept",
        "key": "ctrl+s",
        "when": "phill.diff.isVisible"
      },
      {
        "command": "phill.diff.accept",
        "key": "cmd+s",
        "when": "phill.diff.isVisible"
      }
    ]
  },
  "main": "./dist/extension.cjs",
  "type": "module",
  "scripts": {
    "prepackage": "npm run generate:notices && npm run check-types && npm run lint && npm run build:prod",
    "build": "npm run build:dev",
    "build:dev": "npm run check-types && npm run lint && node esbuild.js",
    "build:prod": "node esbuild.js --production",
    "generate:notices": "node ./scripts/generate-notices.js",
    "prepare": "npm run generate:notices",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "watch": "npm-run-all2 -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "vsce package --no-dependencies",
    "test": "vitest run",
    "test:ci": "vitest run --coverage",
    "validate:notices": "node ./scripts/validate-notices.js"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/node": "20.x",
    "@types/vscode": "^1.99.0",
    "@typescript-eslint/eslint-plugin": "^8.31.1",
    "@typescript-eslint/parser": "^8.31.1",
    "@vscode/vsce": "^3.6.0",
    "esbuild": "^0.25.3",
    "eslint": "^9.25.1",
    "npm-run-all2": "^8.0.2",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.23.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "zod": "^3.25.76"
  }
}
